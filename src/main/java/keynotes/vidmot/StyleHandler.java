package keynotes.vidmot;

import javafx.scene.control.Button;
import javafx.scene.control.ToggleButton;

public class StyleHandler {

/*
        public static void selectedStyle(ToggleButton toggleButton) {
            if (toggleButton.getStyleClass().contains("toolButtonSelected")) {
                toggleButton.getStyleClass().remove("toolButtonSelected");
                toggleButton.getStyleClass().add("toolButton");
            } else {
                toggleButton.getStyleClass().remove("toolButton");
                toggleButton.getStyleClass().add("toolButtonSelected");
                System.out.println("SELECTED- current styleclass : " + toggleButton.getStyleClass());
            }
        }


    public static void addListeners(ToggleButton[] toolKeys) {
        for (ToggleButton toggleButton : toolKeys) {
            toggleButton.selectedProperty().addListener((observable, oldValue, newValue) -> {
                if (newValue) {
                    selectedStyle(toggleButton);
                }
            });
        }
    } */
}
